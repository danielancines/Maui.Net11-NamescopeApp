<ContentPage x:Class="Maui.Net11_NamescopeApp.MainPage"
             xmlns:local="clr-namespace:Maui.Net11_NamescopeApp"
             x:Name="MyMainPage"
             x:DataType="local:MainPage">

    <ContentPage.Resources>
        <DataTemplate x:Key="FirstDataTemplate">
            <Label BindingContext="{Binding Source={x:Reference MyMainPage}}"/>
        </DataTemplate>
        <DataTemplate x:Key="FirstDataTemplate1">
            <Label BindingContext="{Binding Source={x:Reference MyMainPage}}"/>
        </DataTemplate>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a submarine number ten" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
